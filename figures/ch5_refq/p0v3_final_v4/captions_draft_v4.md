# Chapter 5 RefQ Figure Captions (Draft)

These captions are rendering artifacts only. They are not inserted into the
frozen manuscript. Values and labels are taken from the corrected P0-v3 and
corrected supplemental v2 authorities.

## Figure 1. Observable Reference evidence and its project-mappable boundary

RQ1. The flow reports observable Reference records before and after the frozen
source-admission contract: 120 records are excluded as out of seed, and the
admitted records are then separated by target membership status. The source-event
panel shows all eight frozen categories; the compact
95%-coverage presentation rule used by main-text tables is not applied here.
All counts are Reference records. Project-mappable records are an eligibility
boundary and do not by themselves support mechanistic or task-level
interpretation.

## Figure 2. Asymmetric source and target views of observed Project-level RefQ

RQ2a and RQ2b. Source out-degree and out-strength are plotted on separate axes;
the two metrics are not overlaid. Strength is aggregated Reference-record weight.
Target quantiles use coverage
of the 294 observed seed sources, while the concentration panel uses the
frozen cross-project weight denominator of 138,974. The 294 seed sources are
source-complete observations and expanded targets are source-incomplete
observations; no project-value ranking is implied. Panel C uses base-10
logarithmic y-axes only for display; the plotted Q1, median, Q3, and maximum
values remain the original frozen values.

## Figure 3. First-order undirected RefQ structure and seed-sensitive algorithmic modular neighborhood view

RQ2c. The first-order undirected view is summarized together with the three
declared observation boundaries and the 50-run seed sensitivity record. Seed
20260731 is shown as the canonical deterministic reference realization. The
variation in community count and ARI is an explicit limitation: the display is
an algorithmic modular neighborhood view, not a claim about an underlying
stable partition, a technical-subdomain taxonomy, or a consensus result.

## Figure 4. Observed subdomain variation and FDR-bounded role/structure comparisons

RQ3. Frozen means and medians are shown for the four requested descriptive
features under both declared label modes, without uncertainty intervals. In
Panels A-D, circles/squares encode mean/median and filled/open markers encode
include_mixed/exclude_mixed_or_multilabel; Panel E uses filled/open markers only
for BH-FDR reject/not reject. The
effect-size panel reports the executed epsilon-squared and BH-FDR status for
all 11 frozen features. Results are local and label-mode sensitive; category
labels are metadata and are not algorithmic communities or causal mechanisms.

## Supplementary S1. Reference-record multiplicity sensitivity

Thresholds 1, 2, 5, and 10 retain Reference-record multiplicity for an ordered
project pair. The display reports retained directed edges, retained weight
share, and LCC coverage with the exact frozen table. A threshold is not an
event-support, semantic-strength, or optimization claim.

## Supplementary S2. Louvain seed stability under the declared algorithmic view

All 50 frozen runs and the pairwise ARI distribution are shown. Community IDs
are labels of algorithmic runs only and receive no substantive interpretation;
the run-level variation is retained as a qualification on the canonical
reference realization.

## Supplementary S3. Structural brokerage-candidate ranking stability

The panels report only rank-stability diagnostics for structural brokerage
candidates: direct Spearman agreement values, top-50 overlap, and canonical top-50 inclusion
frequency under the declared settings. These diagnostics do not establish
importance, influence, or causal bridging.

## Supplementary S4. Reference-record, aggregated-weight, and edge-count contract

This unit-aware audit separates RECORD counts, REFERENCE_RECORD/aggregated
RefQ weights, and EDGE_COUNT values from the frozen evidence-universe and
edge-class files. The rows are a contract audit and do not share a common
quantitative axis.
